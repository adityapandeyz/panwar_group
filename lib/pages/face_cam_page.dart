import 'package:flutter/material.dart';

import '../widgets/custom_page.dart';

class FaceCam extends StatelessWidget {
  const FaceCam({super.key});

  @override
  Widget build(BuildContext context) {
    return const CustomPage(
      widget: Column(
        children: [],
      ),
    );
  }
}
