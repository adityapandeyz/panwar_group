import 'package:flutter/material.dart';

class NoAccessType extends StatelessWidget {
  const NoAccessType({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
